# UPLABS v2 — Backend env (server-side only, never exposed to client)
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_ROLE_KEY=
# fallback if service role not available
SUPABASE_ANON_KEY=
